// import { computed, reactive } from "vue";
// import { IAuthStore, IUserLogin } from "../models/ILogin";
// // import type { IAuthStore, IGroup, IPerson, IUser } from './../models/ILogin'

// const state = reactive<IAuthStore>({
//   user: null,
//   token: null,
// });

// const authStore = {
//   user: {
//     get: () => state.user,
//     set: (value: IUserLogin) => (state.user = value),
//   },
//   token: {
//     get: () => state.token,
//     set: (value: string) => (state.token = value),
//   },
// };

// export default computed(() => authStore).value;
